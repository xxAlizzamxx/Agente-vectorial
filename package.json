{
  "name": "agente-busqueda-vectorial-codex",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test test/**/*.test.ts",
    "ingest": "tsx src/ingest.ts",
    "search": "tsx src/search.ts",
    "agent": "tsx src/agent.ts"
  },
  "dependencies": {
    "@openai/codex-sdk": "^0.104.0",
    "dotenv": "^17.2.2",
    "openai": "^5.19.1"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  },
  "engines": { "node": ">=18" }
}
